import { useNavigate } from "react-router-dom";
import { useLocation } from "react-router-dom";

export function useRedirectMultipleSlashes() {
  

  // Если нет множественных слешей, рендерим страницу
  return null;
}
